# 08. 結合アルゴリズム

## この章の役割

同じ論理結合を実現する代表的なアルゴリズムを比較し、入力サイズ、インデックス、メモリ、順序が選択へ与える影響を理解する。

## この章で答える問い

- 入れ子ループ結合はいつ有効で、いつ極端に遅くなるのか
- ハッシュ結合の構築側を小さくする必要があるのはなぜか
- ソートマージ結合は、どのような入力で有利になるのか

## 扱う内容

- 単純／ブロック／インデックス入れ子ループ結合
- ハッシュ結合の構築/探索、分割、ディスク退避
- ソートマージ結合のソートとマージ
- 等値結合、範囲結合、非等価条件による制約
- 内側/外側/半結合／反結合と実装上の注意
- 入力の大小、既存の順序、利用可能なインデックスによる選択
- 結合順序が中間結果の大きさへ与える影響

## 図解・具体例

顧客と注文の結合を3アルゴリズムで実行し、ページアクセスと中間データの流れを並べて示す。

## 演習・確認課題

テーブル件数、選択率、メモリ、インデックス有無が異なる複数ケースで結合方式を選ぶ。

## 読了時の到達目標

実行計画で選ばれた結合方式を、入力特性とコストから説明できる。
