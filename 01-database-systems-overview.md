# 01. データベースシステムの全体像

## この章の役割

本書の地図を示す導入章。アプリケーションがSQLを送ってから結果を受け取るまでに、どの構成要素が関わるかを一度俯瞰し、後続章の位置づけを明確にする。

## この章で答える問い

- DBMSは「データを保存する箱」以上に何をしているのか
- ストレージエンジン、クエリエンジン、トランザクションマネージャはどう連携するのか
- 単一ノードDBと分散DBでは、問題がどのように増えるのか

## 扱う内容

- client、connection、parser、optimizer、executor、buffer pool、storageの責務
- control planeとdata planeの大まかな区別
- OLTPとOLAP、および行指向・列指向への入口
- latency、throughput、durability、availability、consistencyの基本的なトレードオフ
- 本書全体で追跡する注文テーブルのサンプル

## 図解・具体例

- `SELECT`と`INSERT`がDB内部を通過する経路図
- メモリ上の処理と永続ストレージ上の処理を色分けした構成図
- 単一ノードからleader/follower構成へ拡張した際の差分図

## 演習・確認課題

身近なWeb APIの1リクエストを選び、DBの各構成要素が担当する処理を推測して図にする。

## 読了時の到達目標

後続章の用語がDBMS全体のどこに位置し、どの問題を解くものかを説明できる。
